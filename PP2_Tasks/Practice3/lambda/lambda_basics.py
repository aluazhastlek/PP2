# quick calculations without creating a named function
square = lambda x: x * x
print("Example 1:", square(7))  # 49

# multiple arguments
area_rect = lambda w, h: w * h
print("Example 1 (multi-args):", area_rect(5, 3))  # 15